import { LogInHistoryLocaleType } from "./types";

export default {
  LogInHistory: "Log-in history",
  YouCan: "You can view a full log-in history in the client’s cabinet. Furthermore, we send you email alerts if we detect suspicious attempts to log in to your account."
} as LogInHistoryLocaleType;
